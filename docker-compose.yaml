version: '3.4'
services:
  api:
    build:
      context: ./API
      dockerfile: Dockerfile
    container_name: api
    ports:
      - "5000:5000"
    environment:
      PYTHONUNBUFFERED: 1